---
import Google from './Google.astro'
import Umami from './Umami.astro'

import { themeConfig } from '@/theme.config'

const provider = themeConfig.analytics.provider
---

{provider === 'google' && <Google />}
{provider === 'umami' && <Umami />}
