---
import { themeConfig } from '@/theme.config'

interface Props {
  title: string
}
const { title } = Astro.props
---

<link rel="alternate" type="application/rss+xml" title={title} href={new URL('atom.xml', themeConfig.site.website)} />
